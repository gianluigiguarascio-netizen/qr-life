/* QR Life — Admin Script */
jQuery(function ($) {
    /* Nessuna logica complessa necessaria nell'admin statico,
       le pagine sono server-rendered. Questo file è riservato
       a future interazioni AJAX dal pannello. */
});
